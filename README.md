required apps: i3 i3status dmenu xorg xwallpaper kitty alacritty picom brightnessctl xclip
optional but recommended apps: i3blocks i3lock

!! If using Pulseaudio instead of Pipewire, change the audio keybinds (from the i3 config) to "pactl" instead of "wpctl"
